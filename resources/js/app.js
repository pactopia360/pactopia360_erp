// resources/js/app.js
// Mantén esto mínimo para evitar dependencias remotas en local.
import './bootstrap';
// Si más adelante necesitas UI global, impórtala aquí (siempre same-origin).
import ApexCharts from "apexcharts";
window.ApexCharts = ApexCharts;
